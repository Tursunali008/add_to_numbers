/// A class to add two numbers and return the result.
class AddToNumbers {
  /// Adds two numbers and returns the result.
  /// 
  /// Returns the sum of [num1] and [num2].
  double addNumbers(double num1, double num2) {
    return num1 + num2;
  }
}
